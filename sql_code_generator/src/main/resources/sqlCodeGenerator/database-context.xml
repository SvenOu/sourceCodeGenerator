<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context.xsd">

    <bean id="sqliteDataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource" primary="true">
        <property name="driverClassName" value="${jdbc.sqlite.driverClassName}"/>
        <property name="url" value="${jdbc.sqlite.url}"/>
        <property name="username" value="${jdbc.sqlite.username}"/>
        <property name="password" value="${jdbc.sqlite.password}"/>
    </bean>


    <bean id="sqlServer2005DataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="driverClassName" value="${jdbc.sqlServer2005.driverClassName}"/>
        <property name="url" value="${jdbc.sqlServer2005.url}"/>
        <property name="username" value="${jdbc.sqlServer2005.username}"/>
        <property name="password" value="${jdbc.sqlServer2005.password}"/>
    </bean>

</beans>